# Internal working documents — kept in git, never uploaded to the CDN.
# tools/ IS uploaded: the build command runs from it. It contains no secrets
# and duplicates logic that is necessarily public in assets/catalog-render.js.
docs/
sql/
PHASE-0-PLAN.md
BREVO-SETUP.md
DIGEST-SETUP.md
DNS-MIGRATION.md
.gitignore
.claude/

# Local diagnostic, run by hand from a terminal. tools/ is otherwise uploaded
# because the build command runs from it, so this one file is named explicitly
# rather than excluding the directory.
tools/brevo-diag.mjs

# Local one-off, same reasoning as brevo-diag above.
tools/resend-welcome.mjs
